## Task: Dispatch (Self-Directed)

You are deciding what to do this session. Check:

1. Recall recent session logs: `recall(tags=["session-log", "perch-time"], n=5)`
2. Check when each task last ran (sleep, zeitgeist, fly)
3. Check for incomplete tasks or pending items in your boot context
4. Decide what's most needed right now

Then execute that task. You have all tools available.

If nothing is pressing, default to sleep (memory maintenance).
